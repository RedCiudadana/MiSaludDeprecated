<div id="wrapper-slider">
  {{!-- slider --}}
  <img
    src="{{rootURL}}/img/slider.png"
    alt=""
    width="100%"
    class="42vw"
    id="start"
  >
  <img
    src="{{rootURL}}/img/ambulance.png"
    alt="Ambulancia"
    class="ambulance animate-to-left"
  >
</div>
{{!-- Light container --}}
<div class="container bg-light">
  <div class="row">
    <div class="col-10 offset-1 filters">
      {{!-- Departamento --}}
      {{one-way-select selectedValue
        options=options
        update=(action (mut selectedValue))
        class="form-control"
      }}
      {{!-- Hospital/Centro de salud --}}
      {{one-way-select selectedValue
        options=options
        update=(action (mut selectedValue))
        class="form-control"
      }}
    </div>
  </div>
</div>
{{!-- White container --}}
<div class="container">
  {{preview-hospital}}
</div>
